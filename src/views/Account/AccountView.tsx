import React from 'react';
import styled from 'styled-components';

interface AccountViewProps {

}

export const AccountView: React.FC<AccountViewProps> = () => {

    return (
        <Container>AccountView</Container>
    );
};

const Container = styled.div`
    padding: 2em;
`;
